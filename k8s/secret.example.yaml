# Copy to secret.yaml, fill values, apply with:
#   kubectl apply -f k8s/secret.yaml
# DO NOT commit secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: grav-secrets
  namespace: grav
stringData:
  GITHUB_TOKEN: "ghp_REPLACE_WITH_PAT_WITH_CONTENTS_WRITE"
